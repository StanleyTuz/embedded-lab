import math

SCALE = ['A4', 'Ax4', 'B4', 'C4', 'Cx4', 'D4', 'Dx4', 'E4', 'F4', 'Fx4', 'G4', 'Gx4', 'F5']

def freq_to_half_period_ticks(freq: float) -> float:
    factor_ = (16 * 10**6) / 2
    return factor_ / freq

ref_freq = 440
factor = math.pow(2, 1/12)

frequencies = [ref_freq]
for i in range(1, 13):
    frequencies.append(frequencies[i-1] * factor)

half_period_ticks = [
    freq_to_half_period_ticks(f) for f in frequencies
]
half_period_ticks = [
    int(hpt) for hpt in half_period_ticks
]

print('frequencies: ', frequencies)
print(half_period_ticks)

def makeDefines(half_period_ticks):
    defineString = "// Half-period tick counts for 16 MHz chip\n// Automatically generated by scaleGenerator.py \n\n"
    for (note, length) in pitchList:
        defineString += "#define  {:<5}{:>6}\n".format(note, length)
    return(defineString)